{
  "name": "jquery-todo",
  "version": "1.0.0",
  "description": "For private education",
  "main": "index.js",
  "scripts": {
    "clean": "rm -rf build",
    "start": "yarn clean && webpack-dev-server --config webpack.config.js --mode development",
    "build": "webpack --config webpack.config.js --mode development",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/sinpaout/jquery-todo.git"
  },
  "author": "",
  "license": "ISC",
  "bugs": {
    "url": "https://github.com/sinpaout/jquery-todo/issues"
  },
  "homepage": "https://github.com/sinpaout/jquery-todo#readme",
  "dependencies": {
    "html-webpack-plugin": "^3.2.0",
    "webpack": "^4.30.0",
    "webpack-dev-server": "^3.3.1"
  },
  "devDependencies": {
    "webpack-cli": "^3.3.0"
  }
}
